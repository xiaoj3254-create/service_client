# 项目运行修复记录

> 日期：2026-09-09
> 环境：Windows + Python 3.13.15 + 项目虚拟环境 `.venv`
> 目标：在虚拟环境中成功运行多智能体客服系统（LangGraph 服务 + Flask Web 应用）

---

## 一、修改文件清单

| 文件 | 修改类型 | 说明 |
| --- | --- | --- |
| `session_manager.py` | 代码修复 | `MongoDBChatMessageHistory` 改为可选导入，兼容新版 langchain-community |
| `langgraph.json` | 配置修复 | 图入口由不存在的 `build_workflow` 修正为实际的 `make_graph` |
| `.venv/`（新增） | 环境搭建 | 新建虚拟环境并安装全部依赖，补装 Windows 必需的 `colorama` |

---

## 二、详细修改内容

### 1. `session_manager.py` —— 修复 MongoDB 历史类导入失败

**问题现象：**

运行 `python multi_agent_customer_service.py` 自检时报错：

```text
ImportError: cannot import name 'MongoDBChatMessageHistory'
from 'langchain_community.chat_message_histories'
```

**原因：**

新版 `langchain-community`（0.4.x）已将 MongoDB 集成迁移至独立的 `langchain-mongodb` 包，
`langchain_community.chat_message_histories` 中不再导出 `MongoDBChatMessageHistory`。
系统默认使用 `memory` 后端，MongoDB 仅为可选后端，不应因该可选依赖缺失而导致整个程序无法启动。

**修改内容：**

- 将 `MongoDBChatMessageHistory` 从顶部强制导入改为 `try/except` 可选导入，
  依次尝试 `langchain_community` → `langchain_mongodb`，均不可用时置为 `None`。
- 在 `_create_chat_history()` 的 `mongodb` 分支中增加判断，缺失时抛出明确的安装提示。

修改后的导入部分：

```python
# 存储后端导入
from langchain_community.chat_message_histories import (
    RedisChatMessageHistory,
    PostgresChatMessageHistory,
    FileChatMessageHistory
)

# MongoDB 后端在新版 langchain-community 中已迁移至独立的 langchain-mongodb 包，
# 属于可选后端，缺失时不影响默认的 memory/redis/postgres/file 后端使用。
try:
    from langchain_community.chat_message_histories import MongoDBChatMessageHistory
except ImportError:
    try:
        from langchain_mongodb import MongoDBChatMessageHistory
    except ImportError:
        MongoDBChatMessageHistory = None
```

mongodb 分支增加保护：

```python
if self.storage_backend == "mongodb":
    if MongoDBChatMessageHistory is None:
        raise ImportError(
            "MongoDB 后端需要安装 langchain-mongodb 包：pip install langchain-mongodb"
        )
    return MongoDBChatMessageHistory(...)
```

---

### 2. `langgraph.json` —— 修正图入口函数名

**问题现象：**

`langgraph dev` 启动失败：

```text
GraphLoadError: Failed to load graph 'customer_service' from
./multi_agent_customer_service.py: Could not find graph 'build_workflow'
...
Found the following exports: ..., classify_query_node, create_agent_node,
final_response_node, make_graph
```

**原因：**

`langgraph.json` 中 `graphs.customer_service` 配置的入口为 `./multi_agent_customer_service.py:build_workflow`，
但该文件实际导出的图工厂函数名为 **`make_graph`**（见 `multi_agent_customer_service.py`），不存在 `build_workflow`。

**修改内容：**

```json
"graphs": {
  "customer_service": "./multi_agent_customer_service.py:make_graph"
}
```

---

### 3. 虚拟环境搭建与依赖补装

**操作：**

1. 在项目根目录创建虚拟环境：

   ```powershell
   python -m venv .venv
   ```

2. 安装项目依赖与 LangGraph CLI：

   ```powershell
   .\.venv\Scripts\python.exe -m pip install -r requirements.txt "langgraph-cli[inmem]"
   ```

3. 补装 `colorama`（Windows 必需，见下方问题）：

   ```powershell
   .\.venv\Scripts\python.exe -m pip install colorama
   ```

**补充说明 —— 为什么需要 `colorama`：**

虚拟环境中首次启动 `langgraph dev` 时报错：

```text
SystemError: ConsoleRenderer with `colors=True` on Windows requires the
colorama package installed.
```

`langgraph-api` 使用 structlog 的彩色控制台渲染器，在 Windows 上依赖 `colorama`，
该包未包含在 `requirements.txt` 中，需手动安装。

---

## 三、验证结果

| 验证项 | 结果 |
| --- | --- |
| 图结构自检 `python multi_agent_customer_service.py` | ✅ 通过（工作流图构建完成） |
| LangGraph 服务 `http://127.0.0.1:2024/ok` | ✅ 200 |
| Flask Web 首页 `http://127.0.0.1:5000/` | ✅ 200 |
| 健康检查 `/api/health` | ✅ healthy |
| LangGraph 连通性 `/api/test` | ✅ 各接口均 200 |
| 端到端对话（产品咨询） | ✅ 正确路由至「产品专家」并返回专业回复 |

---

## 四、启动方式（在项目根目录下）

```powershell
# 终端 1：启动 LangGraph 服务（默认端口 2024）
.\.venv\Scripts\langgraph.exe dev --port 2024 --host 127.0.0.1 --no-browser

# 终端 2：启动 Flask Web 应用（端口 5000）
.\.venv\Scripts\python.exe web_app.py
```

启动后浏览器访问：<http://localhost:5000>

- LangGraph Studio UI：<https://smith.langchain.com/studio/?baseUrl=http://127.0.0.1:2024>
- LangGraph API 文档：<http://127.0.0.1:2024/docs>

---

## 五、第二轮修复：新建对话不隔离问题（2026-09-09）

**问题现象：**

点击左侧「新建对话」后发送消息，所有问答仍累积在同一个对话框里，侧边栏不会产生新的
独立对话条目；切换/新建会话后，历史消息互相串线。

### 修改文件清单

| 文件 | 修改类型 | 说明 |
| --- | --- | --- |
| `chat_web_service.py` | 后端逻辑修复 | `ensure_thread_exists` 不再复用全局缓存线程；空/非法会话 ID 一律新建 LangGraph 线程 |
| `templates/index.html` | 前端逻辑修复 | 新建对话时置空会话 ID；首条消息成功后采用后端返回的真实 `thread_id` |
| `multi_agents/billing_agent.py` | Bug 修复 | 账单信息关键词兜底匹配抛 `IndexError`，导致账单类问题 500 |
| `multi_agents/tech_agent.py` | Bug 修复 | 同上模式（技术信息匹配 `_match_tech_info`） |
| `multi_agents/general_agent.py` | Bug 修复 | 同上模式（服务信息匹配 `_match_service_info`） |

### 1. 后端：线程缓存复用导致「新对话 = 旧线程」

**根因：**

`chat_web_service.py` 的 `ensure_thread_exists()` 中，前端「新建对话」传来的是浏览器本地
编造的 `web_<时间戳>`，并非合法 LangGraph 线程 ID。旧代码验证该 ID 失败后，直接返回全局
缓存的 `_current_thread_id`（即第一条对话创建的线程）：

```python
# 旧逻辑（有问题）
if _current_thread_id:
    return True        # ← 新对话直接复用第一条对话的线程
```

于是每次「新建对话」的消息都写入同一个 LangGraph 线程，对话无法隔离。

**修复：** 合法且存在的线程 ID → 复用（继续/切换历史会话）；为空、`default` 或非法 →
**一律新建线程**，删除缓存复用分支：

```python
def ensure_thread_exists(client_session_id: Optional[str] = None) -> bool:
    """
    - client_session_id 是已存在的合法线程 ID：复用该线程（继续/切换历史会话）。
    - client_session_id 为空、'default' 或非法：一律【新建】线程，
      不复用全局缓存，保证每次“新建对话”相互隔离。
    """
    global _current_thread_id

    sid = client_session_id
    if sid and sid != 'default':
        try:
            thread_response = requests.get(f"{LANGGRAPH_API_URL}/threads/{sid}", timeout=5)
            if thread_response.status_code == 200:
                _current_thread_id = sid
                return True
            else:
                print(f"⚠️ 会话ID {sid} 不是有效的LangGraph线程ID，将创建新线程")
        except Exception as e:
            print(f"⚠️ 验证会话ID {sid} 时出错: {e}，将创建新线程")

    # 无合法 sid：直接新建线程（不再复用 _current_thread_id 缓存）
    ...
```

### 2. 前端：会话 ID 与真实线程 ID 不同步

**根因：**

`templates/index.html` 中 `createNewSession()` 只在浏览器本地生成
`currentSessionId = 'web_' + Date.now()`，而 `/api/chat` 返回的真实 `thread_id`
从未被采用——导致同一会话的后续消息也无法稳定归属到同一线程。

**修复（三处）：**

1. `createNewSession()`：新建对话时 `currentSessionId = null` 并清空对话区，
   真实线程由后端在首条消息时创建。
2. `updateComposerSendReady()` / `sendMessage()`：允许 `currentSessionId` 为空时
   发送首条消息（线程延迟创建）。
3. `sendMessage()` 成功回调：采用后端返回的 `thread_id`：

```javascript
// 首条消息后后端返回真实 LangGraph 线程ID：
// 采用该ID，使同一对话后续消息继续写入同一线程，侧边栏也能正确高亮
if (data.thread_id && data.thread_id !== currentSessionId) {
    currentSessionId = data.thread_id;
    updateComposerSendReady();
}
```

### 3. 智能体信息匹配 `IndexError`（验证时发现的连带 Bug）

测试账单类问题（如「发票怎么开」）时整个 run 失败返回 500。LangGraph 日志：

```text
IndexError: list index out of range
During task with name 'billing_agent'
```

**根因：** `billing_agent.py` / `tech_agent.py` / `general_agent.py` 三个智能体的
关键词兜底匹配中，去重写法为：

```python
if category not in [info.split('【')[1].split('】')[0] for info in matched_info]:
```

第一轮精确匹配的条目格式是 `【分类】...`，但兜底分支追加的条目格式是
`相关服务：分类...`（不含 `【`），循环再次解析时 `split('【')[1]` 越界崩溃。

**修复：** 三个文件统一改为用集合记录已匹配分类，彻底避免字符串解析：

```python
matched_categories = set()
# 精确匹配命中后：matched_categories.add(category)
# 兜底匹配时：
if category in matched_categories:
    continue
```

### 验证结果（端到端）

| 验证项 | 结果 |
| --- | --- |
| 对话1 首条消息（会员套餐咨询） | ✅ 新建线程 A |
| 对话1 追问 | ✅ 复用线程 A（多轮上下文连续，共 4 条消息） |
| 点击「新建对话」后发送（发票问题） | ✅ 新建独立线程 B，账单专家正常响应（共 2 条消息） |
| 侧边栏会话列表 | ✅ 显示两个独立对话条目 |
| 历史隔离 | ✅ 线程 A 历史不含发票内容；线程 B 仅含本轮发票问答 |

---

## 六、第三轮改动：启用 `next_agent` 状态字段（2026-09-09）

**背景：**

`AgentState` 中的 `next_agent: str` 字段此前只声明、初始化（空串），从未被赋值或读取，
分类条件边直接读取 `query_type` 做路由，该字段处于闲置状态。

### 修改文件清单

| 文件 | 修改类型 | 说明 |
| --- | --- | --- |
| `multi_agent_customer_service.py` | 逻辑优化 | 分类节点按 `query_type` 设置 `next_agent`；条件边改读 `next_agent` 路由 |
| `multi_agents/base_agent.py` | 无最终改动 | 曾加过智能体间 LLM 交接逻辑，经评审后移除（见下） |

### 1. 分类节点设置 `next_agent`

`classify_query_node` 在 LLM 分类完成后，将 `query_type` 映射为目标节点名：

```python
QUERY_TYPE_TO_AGENT = {
    "product_info": "product_agent",
    "technical_support": "tech_agent",
    "billing": "billing_agent",
    "complaint": "complaint_agent",
    "general_inquiry": "general_agent",
}
state["next_agent"] = QUERY_TYPE_TO_AGENT.get(query_type, "")
# 超范围护栏：state["next_agent"] = "final_response"
```

### 2. 条件边改读 `next_agent`

`make_graph()` 中 `classify_query` 的条件边由读取 `query_type` 改为读取 `next_agent`：

```python
workflow.add_conditional_edges(
    "classify_query",
    lambda x: x.get("next_agent", "") or "general_agent",
    {
        "product_agent": "product_agent",
        "tech_agent": "tech_agent",
        "billing_agent": "billing_agent",
        "complaint_agent": "complaint_agent",
        "general_agent": "general_agent",
        "final_response": "final_response",
    }
)
```

### 3. 关于「智能体间交接」的尝试与回退（重要决策记录）

过程中曾尝试让智能体处理后再用 LLM 判断是否交接给其他智能体（`_detect_handoff`：
先关键词匹配，后改为 LLM 分诊复核提示词），并加入了智能体间条件边、`visited_agents`
防死循环、交接时撤回当前轮次等机制。

**评审后确认该方向与当前图结构不符，已全部移除：**

当前图结构是三节点直线流转——

```text
classify_query ──▶ 业务智能体 ──▶ final_response
```

消息只会经过「分类 → 智能体 → 最终回答」三个节点，**智能体之间不存在路由**。
因此智能体处理后的 LLM 交接判断属于多余逻辑，已清理：

- `multi_agents/base_agent.py`：删除 `_detect_handoff()`、`AGENT_NODE_MAP`、
  `AGENT_DESCRIPTIONS` 及 `langchain_core.messages` 导入，恢复为改动前内容。
- `multi_agent_customer_service.py`：删除 `AgentState.visited_agents` 字段及初始化、
  `agent_node` 中的交接检测块；智能体出口条件边恢复为直连 `final_response`。

**最终结论：** `next_agent` 仅在**分类节点**发挥作用——作为分类结果到目标节点的
路由信号；智能体处理完不做任何路由判断，直接进入 `final_response`。

### 验证结果

| 验证项 | 结果 |
| --- | --- |
| 图结构自检 `python multi_agent_customer_service.py` | ✅ 通过（工作流图构建完成） |

---

## 七、第四轮改动：智能体按需实例化（2026-09-10）

**背景：**

`create_agent_node` 内部每次执行都调用 `initialize_agents()`，一次性实例化全部 5 个智能体，
但一次图路由只会用到其中 1 个，其余 4 个立即丢弃。

### 修改文件清单

| 文件 | 修改类型 | 说明 |
| --- | --- | --- |
| `multi_agent_customer_service.py` | 重构优化 | `initialize_agents()` → `_AGENT_CLASSES` 映射 + `get_agent()` 按需实例化并缓存 |

### 修改内容

删除 `initialize_agents()`（每次创建全部 5 个智能体），替换为：

```python
# 智能体类映射（按需实例化：一次路由只创建实际需要的那一个智能体）
_AGENT_CLASSES = {
    "product_agent": ProductAgent,
    "tech_agent": TechAgent,
    "billing_agent": BillingAgent,
    "complaint_agent": ComplaintAgent,
    "general_agent": GeneralAgent,
}

# 已实例化的智能体缓存（同进程多次对话复用，避免重复创建）
_agent_instances: Dict[str, Any] = {}

def get_agent(agent_name: str):
    """按需获取智能体实例（首次调用时创建并缓存，后续复用）"""
    if agent_name not in _agent_instances:
        cls = _AGENT_CLASSES.get(agent_name)
        if cls is None:
            return None
        agent = cls()
        agent.set_llm(get_llm())
        agent.set_session_manager(default_session_manager)
        _agent_instances[agent_name] = agent
    return _agent_instances[agent_name]
```

`agent_node` 内改为 `agent = get_agent(agent_name)`。

**效果：** 首次使用某智能体时创建 1 个实例并缓存；后续同进程对话直接复用缓存，不再重复实例化。

### 验证结果

| 验证项 | 结果 |
| --- | --- |
| 图结构自检 `python multi_agent_customer_service.py` | ✅ 通过（工作流图构建完成） |

---

## 八、第五轮改动：侧栏历史对话搜索（2026-09-12）

**背景：**

历史对话逐渐增多后，侧边栏只能按时间顺序浏览，无法快速定位某次对话。
新增侧栏搜索框，按客户输入过的文本实时过滤历史对话。

### 修改文件清单

| 文件 | 修改类型 | 说明 |
| --- | --- | --- |
| `chat_web_service.py` | 后端增强 | `fetch_sessions_list` 每个会话增加 `search_text` 字段（全部用户消息拼接，限长 1000 字符） |
| `templates/index.html` | 前端功能 | 侧栏标题下方新增搜索框；缓存会话列表后在前端做实时包含匹配，无额外请求 |

### 1. 后端：会话列表增加搜索素材 `search_text`

拉取每个线程的 state、解析历史后，把其中所有**用户消息**拼接为 `search_text`，
限长 1000 字符；在 try 外预设空串，保证任何异常分支字段都有定义：

```python
search_text = ""
try:
    # ... 拉取线程 state、解析 parsed_hist ...
    user_texts = [
        str(m.get("content", "")).strip()
        for m in parsed_hist
        if m.get("is_user") and str(m.get("content", "")).strip()
    ]
    search_text = "\n".join(user_texts)[:1000]
except Exception:
    message_count = 0
    search_text = ""

sessions.append({
    ...,
    "search_text": search_text,
})
```

### 2. 前端：搜索框 + 纯前端缓存过滤

- 搜索框位于侧边栏标题与会话列表之间（放大镜图标 + 清除按钮），
  id 分别为 `sessionSearchInput` / `sessionSearchClear`。
- `refreshSessionList()` 拉取列表后缓存到全局 `allSessions`，再调用
  `renderSessionsBySearch()` 渲染；**不新增任何后端请求**。
- `renderSessionsBySearch()` 将搜索词与会话的 `search_text`
  做小写包含匹配，`search_text` 为空时回退匹配 `last_user_question`；
  无命中显示空状态「未找到匹配的对话」。
- 输入实时过滤；× 按钮或清空输入后恢复完整列表。
- 发送新消息后本就会触发 `refreshSessionList()`，搜索过滤随之自动刷新。

### 验证结果（浏览器实测）

| 验证项 | 结果 |
| --- | --- |
| 搜索框存在、控制台无错误 | ✅ |
| 输入关键词实时过滤会话列表 | ✅ |
| 无匹配词显示「未找到匹配的对话」 | ✅ |
| × 清除后列表恢复 | ✅ |

---

## 九、第六轮改动：客户图片上传（2026-09-12）

**背景：**

客户咨询技术故障时，一张报错截图比文字描述更准确。模型（`.env` 中 `mimo-v2.5`，
base_url `https://api.xiaomimimo.com/v1`）经官方文档确认原生支持图片理解，
采用 OpenAI 兼容的 `image_url` + base64 data URL 多模态消息格式。

### 设计要点

1. **单图、data URL 传输**：前端压缩为 data URL 放入 JSON，不落服务器磁盘。
2. **分类器保持纯文本**：图片只传给路由命中的业务智能体，节省视觉 token。
3. **图片随 `persisted_dialogue` 持久化**，切换会话/刷新页面后历史气泡仍能回显缩略图。
4. **支持纯图片消息**：无文字时前端用占位文本「（图片消息）」保证分类器与侧栏预览可用，
   但聊天气泡和历史回显均不显示该占位文字。

### 修改文件清单

| 文件 | 修改类型 | 说明 |
| --- | --- | --- |
| `multi_agent_customer_service.py` | 图状态/持久化 | `AgentState` 增加 `customer_image`；分类节点初始化；用户轮次写入 `image` 键 |
| `multi_agents/base_agent.py` | 多模态消息 | 新增 `_build_human_message()`，有图时构造 text+image_url 多模态消息 |
| `multi_agents/{product,tech,billing,complaint,general}_agent.py` | 调用替换 | 5 个智能体的 `HumanMessage(...)` 统一改为 `self._build_human_message(..., state)` |
| `chat_web_service.py` | 接口透传 | `run_chat_sync()` / `stream_chat_events()` 增加 `image` 参数并入 run input；`append_turn_from_state()` 透传轮次 `image` |
| `web_app.py` | 入参校验 | 新增 `validate_image_data()`：data URL 前缀、base64 合法性、解码后 ≤ 5MB，非法返回 400 |
| `templates/index.html` | 前端功能 | 图片按钮、隐藏 file input、canvas 压缩、预览条、发送联动、气泡/历史图片渲染 |

### 1. 图状态与持久化

```python
# AgentState
# 客户上传的图片（data URL）；无图时为 None。当前支持单图。
customer_image: Optional[str]

# classify_query_node 中初始化
if "customer_image" not in state:
    state["customer_image"] = None

# 用户轮次持久化（有图才写 image 键，旧数据完全兼容）
user_turn = {"content": ..., "is_user": True, "timestamp": ...}
if state.get("customer_image"):
    user_turn["image"] = state["customer_image"]
```

### 2. 业务智能体构造多模态消息（base_agent）

```python
def _build_human_message(self, text, state=None) -> HumanMessage:
    image = (state or {}).get("customer_image")
    if image:
        return HumanMessage(content=[
            {"type": "text", "text": text},
            {"type": "image_url", "image_url": {"url": image}},
        ])
    return HumanMessage(content=text)
```

`OpenAICompatibleClient.invoke()` 对 list 形式的 content 原样透传，无需额外改动。

### 3. 后端入参校验（web_app.py）

`/api/chat` 与 `/api/chat/stream` 均先调用 `validate_image_data()`：
必须以 `data:image/` 开头、含 `;base64,`、可被严格解码、解码后 ≤ 5MB，
任一不满足直接返回 HTTP 400 与中文错误提示。

### 4. 前端图片处理（templates/index.html）

- 输入框工具栏新增图片按钮 `imageUploadBtn` + 隐藏的 `imageFileInput`
  （accept：PNG/JPEG/WebP/GIF，原图 ≤ 10MB）。
- `compressImageFile()` 原生 canvas 压缩，**无第三方依赖**：
  最长边 ≤ 1280px；照片类输出 JPEG quality 0.8；PNG/WebP 保留透明通道；
  GIF 为保动画直接读取不压缩。
- 选中后 `pendingImage` 保存 data URL，输入框上方显示缩略图预览条，可 × 移除；
  发送按钮就绪条件改为「有文本 **或** 有待发图片」。
- 发送时把 `image` 加入请求体，成功后清空预览；用户气泡渲染缩略图，
  点击在新窗口查看大图；`displaySessionHistory()` 读取历史轮次的 `image` 回显，
  纯图片轮次抑制「（图片消息）」占位文字。

### 验证结果

后端（Python requests 端到端）：

| 验证项 | 结果 |
| --- | --- |
| 非法图片字段（非 data URL） | ✅ HTTP 400 拒绝 |
| 红蓝分色块测试图 + 故障咨询 | ✅ 路由至「技术支持专家」，准确描述「左半红、右半蓝、中间垂直分界线」并给出排查建议 |
| 图片写入会话历史（轮次含 `image`） | ✅ |
| 纯图片消息（占位文本 + 图片） | ✅ HTTP 200 正常回复 |
| 会话列表 `search_text` 字段 | ✅ |

浏览器实测：

| 验证项 | 结果 |
| --- | --- |
| 图片按钮点击无报错、隐藏 file input 存在 | ✅ |
| 设置待发图片后预览条出现、发送按钮高亮 | ✅ |
| × 移除后预览消失、发送按钮恢复禁用 | ✅ |
| 图片+文字发送，用户气泡显示缩略图并收到回复 | ✅ |
| 刷新页面后历史气泡仍显示图片缩略图 | ✅ |
| 纯文本消息回归、控制台无红色错误 | ✅ |

---

## 十、备注

- 测试过程中曾通过 PowerShell `Invoke-WebRequest` 发送中文消息，出现中文被编码为 `?`
  导致分类器误判为 `out_of_scope` 的现象。这是 **PowerShell 命令行编码问题**，
  浏览器前端以 UTF-8 正常发送消息，不受影响；用 Python `requests` 验证对话链路完全正常。
- **Windows GBK 编码坑（2026-09-12 确认）：** 项目源码含大量中文，直接运行
  `langgraph dev` 可能因默认 GBK 读取源码报 `UnicodeDecodeError: gbk codec`。
  启动 LangGraph 前必须先设置 `$env:PYTHONUTF8=1`（Flask 端同理建议设置）：

  ```powershell
  $env:PYTHONUTF8=1
  .\.venv\Scripts\langgraph.exe dev --port 2024 --host 127.0.0.1 --no-browser
  ```
- PowerShell 不支持 bash 风格的 heredoc（`python - <<'PY'` 会报语法错误），
  执行多行 Python 验证脚本时应先写入 `.py` 文件再运行。
- 当前默认使用内存（memory）会话后端；如需 Redis / MongoDB / PostgreSQL / 文件后端，
  请参照 `session_manager.py` 安装对应依赖（MongoDB 需 `pip install langchain-mongodb`）。
